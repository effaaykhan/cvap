# Security reviewer memory

- [Recurring findings](recurring-findings.md) — 116 defect classes: self-asserted fields, intra-tenant IDOR, rolled-back refusal records, guards defeated on the way out, size caps the library ignores, non-CLOEXEC fds inherited by sibling engines, trust material composed per-host and checked host-agnostically, evidence written on a branch that disclaims authority and read by a trust root, grades assigned by the path the attacker chooses, refusals parked in a queue nothing reads, exemptions whose precondition expires, per-scope counters kept as one scalar, parking that holds only the evidence that caused it, refusals justified by a rare event that fire on a scheduled one, counters that only count up beside a relationship that expires, scope filters read from a write-once payload, tests that assert the precondition and stop one line short of the claim, one relation named two ways (first-seen scope vs scope-now), carve-outs that delete a refusal something else was leaning on, corroboration the attacker mints a scan earlier or echoes from a public value, refusals held in per-connection memory that a reconnect rehydrates away, one refusal's rollback discarding another's record, guards matching on the encoded form of hostile JSON, a quarantine scoped to the sweep batch instead of the address it protects, health aggregates that filter out the rows they cannot read while the UI chip stays green, "was the rightful holder here" read from a table only the success path writes, continuity facts the victim's own live answers satisfy, DO NOTHING upserts after an irreversible retire, close-out verbs wider than the decision that fired them, trust exclusions keyed on the verdict label the attacker can decline, parks whose escape hatch is a public value, down migrations that relabel a refusal marker, guards that keep the row alive but not the freshness the decision reads, parks with no expiry and no verb, stated weaknesses stronger than stated, expiries that re-arm from the evidence they release, freshness keyed on not-held instead of contradicts, "two sites" where the second is downstream of the first, carve-outs the freshness read does not repeat so the victim's own renewal sustains the refusal, per-service measurement maps with two values on one service, inet equality that includes the netmask, a "differs from what we hold" refusal where the slot holds two values so the victim's own key sustains its park, operator-facing notes that state the opposite of the code, ageing rules suspended with no freshness bound so a third host inherits the hold, close-outs only the protected party can trigger, backstop indexes keyed on JSON the submitter spells, queue rows naming nobody that no transition can close, two branches for one shape where the cheaper one grants more trust, an early return that skips the verdict switch and the sibling branch's guards, a canonicalisation guard whose claim is wider than its parser, a park that freezes the freshness a later eviction reads so the attacker mints the precondition and the slower path grants more authority, a trust exclusion added to one reader of a row and not its sibling that enforces "the same bar", a uniqueness backstop coarser than the decision layer's conflict unit so the refused write arrives as an abort and the group retries for ever, two guards whose preconditions are each other's consequence, a trust read keyed on the port while the key's identity is (port, protocol), a DO NOTHING record leaving a silent keyless asset that can never re-merge, an operator "word" verb naming no object so it blesses every eligible row including ones written after the operator looked, a guard that turns a crash into a clean refusal without adding the remedy, a list cap applied after the whole set is in memory, an "exactly what you looked at" check that forces the operator to bless every member of a set the attacker helps compose, an escape hatch sized for one instance of the shape it escapes, a choice grammar naming the value and not the service so first-match binds it where the attacker wants and the only accepted answer is theirs, one fact canonicalised twice (SQL lower()/btrim vs Go TrimSpace+ToLower), a guard evaluated after a narrowing filter while the operator's question was computed before it, an address-scoped verb beside a page-scoped screen so the operator blesses keys never rendered and loses the candidate button, a guard that skips rows whose column is NULL when nothing makes it non-NULL, one verb taught to name what the operator saw while its sibling names only the container (the render→click window), a cap on the rows the screen renders and none on the aggregate the decision turns on, a decision cap that is a prefix of the set its guard claims to cover so parking order decides which answer is accepted, each entry capped and the entry count not (with the remedy too big for the request cap), a defensive skip that marks the work done before the refusal, a "no bound" sentinel the request can legally carry (a zero-time `seen_through`), a sole-remedy screen that is one unpaged page ordered by a key the attacker refreshes with no total, two caps nobody multiplied (200 entries x 200 values x 200 groups = 400 MiB for one GET), a page-wide LIMIT spent in attacker-chosen address order that blanks another group's "what this verb destroys" line, a create-and-take verb that records nothing and still takes the address off the real holder, a pre-check that predicts the outcome instead of enforcing it across a READ COMMITTED snapshot boundary, a cap over a JOIN beside a total over one side of it so the console's truncation warning never fires, a per-key refusal applied only to the first item carrying the key, a gate whose cost depends on database state so it runs differently where it runs.
- [Store review harness](store-review-harness.md) — reaching the dev DB, seeding a claimable job, reusing store/dispatch/api/scanpoint/correlate test helpers, driving the lease sweeper, proving a finding is a regression, why `-race` is unavailable.
- [Contract-only review posture](contract-only-review-posture.md) — review proto/schema commits for "does it force the secure implementation", not exploitability today.
- [Store guard vs return value](defect-store-guard-vs-return-value.md) — the SQL refuses the write, the caller returns it anyway and downstream acts on it; plus what a now-sticky value does to confidence.
- [SQL subselect guard race](sql-subselect-guard-race.md) — an UPDATE guarded by an uncorrelated subselect is not race-safe; the inline CASE is. Measured both ways.
- [Probing store and advisory paths](probing-the-store-and-advisory-paths.md) — store-test silently skips every advisory suite without the import role URL; how to probe and clean up.
- [Attacker-durable attribution](attacker-durable-attribution.md) — /etc/os-release is permanent under ADR-095: what the two-site token grammar and the ingest gate now close, and the lies that survive.
- [Working tree moves mid-review](working-tree-moves-mid-review.md) — another agent may edit the same files while you review; re-diff before reporting.
